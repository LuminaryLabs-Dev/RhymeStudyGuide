{
  "name": "rhyme-study-guide",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A static, progressively enhanced MVVM reading experience",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate.mjs",
    "start": "node scripts/serve.mjs"
  }
}
