export const esc = value => String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export const arrow = '<span aria-hidden="true">↗</span>';
export const button = (href,label,secondary=false) => `<a class="button ${secondary?'button-secondary':''}" href="${esc(href)}">${esc(label)} ${arrow}</a>`;
export const eyebrow = text => `<p class="eyebrow">${esc(text)}</p>`;
export const heading = (label,title,description='') => `<div class="section-heading" data-reveal>${eyebrow(label)}<h2>${esc(title)}</h2>${description?`<p>${esc(description)}</p>`:''}</div>`;
export const lessonCards = lessons => `<div class="lesson-list">${lessons.map(l=>`<a class="lesson-card" href="${esc(l.href)}" data-reveal><span class="lesson-number">${esc(l.step)}</span><div><span class="eyebrow">${esc(l.time)}</span><h3>${esc(l.title)}</h3><p>${esc(l.subtitle)}</p></div>${arrow}</a>`).join('')}</div>`;
export const themeCards = themes => `<div class="theme-grid">${themes.map(t=>`<a class="theme-card theme-${esc(t.slug)}" href="${esc(t.href)}" data-reveal><span class="theme-decoration" aria-hidden="true"></span><div>${eyebrow(t.eyebrow)}<h3>${esc(t.title)}</h3><p>${esc(t.description)}</p><span class="text-link">Explore this idea ${arrow}</span></div></a>`).join('')}</div>`;
export const termLinks = terms => `<div class="term-chips">${terms.map(t=>`<a href="${esc(t.href)}">${esc(t.title)} ${arrow}</a>`).join('')}</div>`;
export const bookCards = books => `<div class="book-grid">${books.map(b=>`<article class="book-card" data-reveal>${b.cover?`<img src="${esc(b.cover)}" alt="Cover of ${esc(b.title)}" width="320" height="440" loading="lazy">`:''}<h3><a href="${esc(b.href)}">${esc(b.title)}</a></h3><p>${esc(b.summary)}</p>${button(b.href,'Explore this book',true)}</article>`).join('')}</div>`;
export const questions = items => `<div class="question-list">${items.map((q,i)=>`<details data-question><summary><span class="question-number">${String(i+1).padStart(2,'0')}</span><span>${esc(q.title??q)}</span><span class="plus" aria-hidden="true">+</span></summary><div class="question-answer"><p>${esc(q.hint??'Find a detail in your reading that supports your thinking. Try another interpretation, too.')}</p></div></details>`).join('')}</div>`;
export const breadcrumbs = (vm,items) => `<nav class="breadcrumbs" aria-label="Breadcrumb"><a href="${vm.link('')}">Home</a>${items.map(x=>`<span aria-hidden="true">/</span>${x.href?`<a href="${esc(x.href)}">${esc(x.title)}</a>`:`<span aria-current="page">${esc(x.title)}</span>`}`).join('')}</nav>`;
