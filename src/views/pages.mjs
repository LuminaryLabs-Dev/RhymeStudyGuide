import {esc,arrow,button,eyebrow,heading,lessonCards,themeCards,termLinks,bookCards,questions,breadcrumbs} from '../components/html.mjs';

function scene(v,small=false) {return `<div class="book-scene ${small?'scene-small':''}" aria-hidden="true"><div class="scene-ring ring-one"></div><div class="scene-ring ring-two"></div><div class="scene-cross cross-one">✦</div><div class="scene-cross cross-two">✧</div><span class="orbit-label">A WORLD BETWEEN THE LINES</span><div class="scene-layer book-layer" data-depth="14"><img src="${v.link('assets/open-book.webp')}" alt="" width="1200" height="800" fetchpriority="high"></div><div class="scene-layer sprig-layer" data-depth="28"><img src="${v.link('assets/botanical.webp')}" alt="" width="600" height="900"></div><span class="scene-caption">OPEN A BOOK. OPEN A POSSIBILITY.</span></div>`;}
const pageTop = (v,label=v.kind) => `${breadcrumbs(v,[{title:v.title}])}<header class="page-heading" data-reveal>${eyebrow(label)}<h1>${esc(v.title)}</h1><p class="lead">${esc(v.description)}</p></header>`;
const note = (v,key) => `<section class="reflection" data-reveal><div>${eyebrow('Your reading notebook')}<h2>Leave a thought for later.</h2><p>Your note stays in this browser on this device. It isn’t sent anywhere. Anyone using this browser profile may see it.</p></div><div><label for="reflection">What stayed with you?</label><textarea id="reflection" data-note="${esc(key)}" rows="5" maxlength="8000" placeholder="A detail, a connection, a question…"></textarea><div class="note-actions"><span role="status" data-note-status>Notes save as you type.</span><button class="text-button" data-clear-note type="button">Clear this note</button></div></div></section>`;
const next = (v,items=v.lessons) => `<section class="section">${heading('Keep your curiosity going','A good next step.')}${lessonCards(items)}</section>`;

const views={
 home:v=>`<section class="hero"><div class="hero-copy" data-reveal>${eyebrow('Dylan’s Books · A companion for curious readers')}<h1>A little curiosity.<br>A <em>deeper</em><br>reading.</h1><p class="lead">There’s more to a story than the last page. Explore the words, questions, and ideas that stay with you.</p><div class="actions">${button(v.link('study-guide/'),'Start exploring')}${button(v.link('books/'),'Discover the books',true)}</div><p class="hero-footnote"><span aria-hidden="true">✦</span> For a quiet afternoon. Or a room full of ideas.</p></div>${scene(v)}</section><div class="chapter-divider"><span>01 / OPEN THE DOOR</span><span>A slower look. A richer story.</span><a href="#begin" aria-label="Explore ways to begin">↓</a></div><section id="begin" class="section begin-section"><div>${heading('Make yourself at home','Every reader has<br>a way in.'.replace('<br>',' '),'Start with a page, a question, or an idea. There’s no wrong door.')}<a class="text-link" href="${v.link('study-guide/reading-practice/')}">Find your first reading activity ${arrow}</a></div><div class="entry-list">${[['01','Read a little closer','Simple activities to help you notice more.','study-guide/'],['02','Follow an idea','Explore belonging, change, and perspective.','themes/'],['03','Find the right word','A small vocabulary for bigger discoveries.','vocabulary/']].map(([n,t,d,h])=>`<a href="${v.link(h)}" data-reveal><span>${n}</span><div><h3>${t}</h3><p>${d}</p></div>${arrow}</a>`).join('')}</div></section><section class="question-feature" data-reveal>${eyebrow('A question to take with you')}<p class="display-question">“What did you notice<br>the <em>second</em> time?”</p><p>Sometimes the best discoveries begin with another look.</p>${button(v.lessons[0].href,'Try a closer reading',true)}</section><section class="section">${heading('Explore by idea','Find a thread. Follow it.','These are lenses for your reading—not claims about any particular book.')}${themeCards(v.themes)}</section><section class="closing-banner" data-reveal><span aria-hidden="true">✦</span><div><h2>One book. Many ways to see.</h2><p>Bring a question to your next conversation.</p></div>${button(v.link('discussion/'),'Start a discussion',true)}</section>`,
 books:v=>`${pageTop(v,'The bookshelf')}${v.books.length?bookCards(v.books):`<section class="books-welcome" data-reveal><div>${eyebrow('Dylan’s Books')}<h2>A place for the stories<br>still to come.</h2><p>Book introductions and chapter companions will appear here as they become available. In the meantime, bring a book you love and begin exploring.</p>${button(v.link('study-guide/reading-practice/'),'Explore with your own book')}</div>${scene(v,true)}</section>`}${next(v)}`,
 study:v=>`${pageTop(v,'The study guide')}<section class="intro-split" data-reveal><h2>A companion,<br>not a test.</h2><div><p>Bring any book you’re reading. Choose a short passage, slow down, and let a question guide you. You don’t need to finish an activity in one sitting.</p><p>These original reading-practice activities work with your own book. They do not quote or summarize Dylan’s books.</p>${button(v.link('study-guide/reading-practice/'),'Begin the reading path')}</div></section><section class="section">${heading('Three ways to go deeper','Start wherever curiosity takes you.')}${lessonCards(v.lessons)}</section>${v.books.length?`<section class="section">${heading('Book companions','Choose a book.')}${bookCards(v.books)}</section>`:''}<section class="quiet-panel">${eyebrow('Reading together?')}<h2>Make room for another voice.</h2><p>A good conversation can begin with one detail—and two different ways of seeing it.</p>${button(v.link('discussion/'),'Find a discussion prompt',true)}</section>`,
 practice:v=>`${pageTop(v,'Reading practice · Use with any book')}<p class="content-note">Original learning activities. Keep your own book beside you; no book excerpts are reproduced here.</p>${lessonCards(v.lessons)}<section class="section intro-split"><h2>A little preparation.</h2><div><p>Choose a short passage. Keep a pencil or your reading notebook nearby. Read at a pace that gives you room to think.</p><p>Follow the path in order, or choose the activity that interests you. There’s no score and no timer.</p>${button(v.link('resources/reading-sheet/'),'Open a printable notebook',true)}</div></section>`,
 lesson:v=>`${breadcrumbs(v,[{title:'Study guide',href:v.link('study-guide/')},{title:'Reading practice',href:v.link('study-guide/reading-practice/')},{title:v.lesson.title}])}<header class="lesson-heading" data-reveal>${eyebrow(`Reading practice ${v.lesson.step} · ${v.lesson.time}`)}<h1>${esc(v.title)}</h1><p class="lead">${esc(v.lesson.subtitle)}</p><p class="content-note">Use with your own book · Original reading activity</p></header><div class="reading-layout"><aside class="reading-nav"><p class="eyebrow">On this page</p><a href="#read">Begin here</a><a href="#questions">Think about it</a><a href="#connections">Look closer</a><a href="#reflection">Your reflection</a><button class="text-button" data-print type="button">Print this activity ↗</button></aside><article class="reading-content"><section id="read"><p class="reading-intro">${esc(v.description)}</p>${v.lesson.stages.map((s,i)=>`<div class="reading-step" data-reveal><span>${String(i+1).padStart(2,'0')}</span><div><h2>${esc(s.title)}</h2><p>${esc(s.text)}</p></div></div>`).join('')}</section><section id="questions" class="section">${heading('Think about it','Let a question lead.','Open a question for a gentle nudge. There isn’t a single required answer.')}${questions(v.lesson.questions)}</section><section id="connections" class="section">${heading('Look closer','Give your idea a little room.')}${termLinks(v.relatedTerms)}<p class="related-theme">Explore the idea of <a href="${v.relatedThemes[0].href}">${esc(v.relatedThemes[0].title)} ${arrow}</a></p></section></article></div>${note(v,v.lesson.slug)}<nav class="chapter-navigation" aria-label="Reading activity navigation">${v.previous?button(v.previous.href,`Previous: ${v.previous.title}`,true):button(v.link('study-guide/reading-practice/'),'All reading activities',true)}${v.next?button(v.next.href,`Next: ${v.next.title}`):button(v.link('discussion/'),'Take it into conversation')}</nav>`,
 themes:v=>`${pageTop(v,'Ideas worth exploring')}${themeCards(v.themes)}<section class="intro-split section"><h2>Not an answer.<br>A way of looking.</h2><div><p>A theme can begin as a question. Use these lenses with your own reading, and let the details in the text guide what you discover.</p><p>These explorations are general reading companions, not a list of confirmed themes in Dylan’s books.</p></div></section>`,
 theme:v=>`${breadcrumbs(v,[{title:'Themes',href:v.link('themes/')},{title:v.title}])}<header class="theme-detail theme-${v.theme.slug}" data-reveal>${eyebrow(v.theme.eyebrow)}<h1>${esc(v.title)}</h1><p class="lead">${esc(v.description)}</p><span aria-hidden="true" class="theme-detail-mark">${v.theme.mark}</span></header><section class="intro-split section"><h2>${esc(v.theme.question)}</h2><div><p>${esc(v.theme.approach)}</p><p class="content-note">A lens for any book you’re reading.</p></div></section><section class="quiet-panel">${eyebrow('Try this with your book')}<h2>${esc(v.theme.prompt)}</h2>${button(v.relatedLessons[0].href,'Explore through an activity')}</section><section class="section">${heading('Words to help you look closer','A little language for your thinking.')}${termLinks(v.relatedTerms)}</section>${v.relatedBooks.length?bookCards(v.relatedBooks):''}${next(v,v.relatedLessons)}`,
 vocabulary:v=>`${pageTop(v,'The word companion')}<p class="content-note">A small glossary of reading terms. Browse at your own pace.</p><div class="vocabulary-list">${v.terms.map(t=>`<a href="${t.href}" data-reveal><span class="vocabulary-letter" aria-hidden="true">${t.title[0]}</span><div><h2>${esc(t.title)}</h2><p>${esc(t.definition)}</p></div>${arrow}</a>`).join('')}</div>`,
 term:v=>`${breadcrumbs(v,[{title:'Vocabulary',href:v.link('vocabulary/')},{title:v.title}])}<header class="page-heading word-heading" data-reveal>${eyebrow('A word worth knowing')}<h1>${esc(v.title)}<span class="word-period">.</span></h1><p class="lead">${esc(v.description)}</p></header><section class="intro-split section"><h2>In your reading.</h2><div><p>${esc(v.term.context)}</p></div></section><section class="quiet-panel">${eyebrow('An original example to think about')}<h2>${esc(v.term.example)}</h2></section>${v.relatedThemes.length?`<section class="section">${heading('Make a connection','Where could this word take you?')}${themeCards(v.relatedThemes)}</section>`:next(v,[v.lessons[0]])}`,
 discussion:v=>`${pageTop(v,'Around the reading table')}<section class="prompt-explorer" data-reveal><div class="prompt-tabs" role="group" aria-label="Choose a discussion prompt">${v.prompts.map((p,i)=>`<button type="button" data-prompt-index="${i}" aria-pressed="${i===0}">${esc(p.category)}</button>`).join('')}</div><div class="prompt-stage" aria-live="polite" aria-atomic="true">${v.prompts.map((p,i)=>`<div class="prompt-panel" data-prompt-panel="${i}" ${i?'hidden':''}>${eyebrow('An invitation to talk')}<h2>${esc(p.text)}</h2><p>${esc(p.followup)}</p><a class="text-link" href="${v.link(`study-guide/reading-practice/${p.lesson}/`)}">Try the related reading activity ${arrow}</a></div>`).join('')}</div><button type="button" class="button button-secondary" data-next-prompt>Another question <span aria-hidden="true">↻</span></button></section><section class="section intro-split"><h2>Listen for something<br>you haven’t seen yet.</h2><div><p>Give everyone room to think before answering. Invite different interpretations, ask for a detail from the text, and let people pass when they prefer to listen.</p>${button(v.link('resources/discussion-sheet/'),'Open the discussion sheet',true)}</div></section><noscript><section class="section"><h2>More questions</h2>${questions(v.prompts.map(p=>({title:p.text,hint:p.followup})))}</section></noscript>`,
 resources:v=>`${pageTop(v,'For your reading table')}<div class="resource-grid">${[['01','A closer-reading notebook','Space for a passage, a telling detail, a connection, and a question.','reading-sheet'],['02','A conversation guide','Thoughtful prompts and a simple structure for reading together.','discussion-sheet']].map(([n,t,d,h])=>`<article class="resource-card" data-reveal><span class="resource-number">${n}</span>${eyebrow('Print-friendly · No sign-in')}<h2>${t}</h2><p>${d}</p>${button(v.link(`resources/${h}/`),'Open printable sheet',true)}</article>`).join('')}</div><section class="quiet-panel"><h2>Paper, pencil, possibility.</h2><p>Open a sheet and choose Print to make a paper copy or save a PDF using your browser. The reading activities are also print-friendly.</p></section>`,
 worksheet:v=>`${pageTop(v,'Print-friendly reading companion')}<div class="print-toolbar"><button class="button" type="button" data-print>Print or save as PDF ${arrow}</button><p>Use your browser’s print menu to choose paper or PDF.</p></div><article class="worksheet"><div class="worksheet-meta"><p>Book: <span></span></p><p>Passage / page: <span></span></p><p>Date: <span></span></p></div>${[['Notice','A word, image, or moment that caught my attention.'],['Look closer','What this detail suggests—and the evidence for my thinking.'],['Connect','Another moment, idea, or experience this connects to.'],['Wonder','A question I want to carry into my next reading.']].map(([t,d])=>`<section><h2>${t}</h2><p>${d}</p><div class="writing-lines" aria-hidden="true"></div></section>`).join('')}</article>`,
 'discussion-sheet':v=>`${pageTop(v,'Print-friendly conversation companion')}<div class="print-toolbar"><button class="button" type="button" data-print>Print or save as PDF ${arrow}</button></div><article class="worksheet"><p>Choose a passage together. Take a quiet moment to think. Invite each reader to share or pass.</p>${v.prompts.map(p=>`<section><h2>${esc(p.category)}</h2><p>${esc(p.text)}</p><p>${esc(p.followup)}</p><div class="writing-lines short" aria-hidden="true"></div></section>`).join('')}<p>Close with one question you would like to return to.</p></article>`,
 about:v=>`${pageTop(v,'Dylan’s Books')}<section class="intro-split section"><h2>For the reader<br>who asks “why?”</h2><div><p>Rhyme Study Guide is a home for exploring Dylan’s Books through close reading, questions, and conversation.</p><p>Start with an original reading activity, bring your own book, and follow an idea at your own pace.</p><div class="actions">${button(v.link('about/project/'),'About the guide')}${button(v.link('about/author/'),'About Dylan',true)}</div></div></section>${next(v,[v.lessons[0]])}`,
 author:v=>`${pageTop(v,'The author')}<section class="intro-split section"><h2>Behind the books.</h2><div><p>${esc(v.author.biography??'This is the author page for Dylan’s Books. A fuller introduction to Dylan and the writing will be added here.')}</p><p>For now, the reading companion is open. Bring a book and a little curiosity.</p>${button(v.link('books/'),'Visit the bookshelf',true)}</div></section>${next(v,[v.lessons[0]])}`,
 project:v=>`${pageTop(v,'About Rhyme Study Guide')}<article class="prose"><h2>Read with room to think.</h2><p>This guide supports a simple habit: return to a detail, ask a better question, and see what changes. It welcomes independent readers, families, teachers, and reading groups.</p><h2>Start with what you’re reading.</h2><p>The reading-practice activities, general themes, vocabulary definitions, and discussion prompts can be used with any book. They are original learning materials, not excerpts, summaries, or confirmed interpretations of Dylan’s books.</p><h2>Keep your own pace.</h2><p>Activities have no scores or deadlines. Your reflections can stay in this browser, or you can use a printable sheet. The site does not send your notes to a server.</p><h2>A comfortable reading space.</h2><p>You can turn off decorative motion using the Motion button. Your device’s reduced-motion preference is respected. Reading material and navigation remain available without animation.</p><h2>Credits</h2><p>Created for Dylan’s Books by Luminary Labs. Decorative book and botanical illustrations are AI-generated and do not represent published book covers.</p>${button(v.link('study-guide/'),'Find a place to begin')}</article>`,
 'not-found':v=>`${pageTop(v,'Page not found')}<div class="actions">${button(v.link(''),'Return home')}${button(v.link('study-guide/'),'Explore the study guide',true)}</div>`,
 book:v=>`${pageTop(v,v.book.author)}<article class="prose">${v.book.cover?`<img class="book-cover" src="${esc(v.book.cover)}" alt="Cover of ${esc(v.book.title)}">`:''}<h2>Inside this book</h2><p>${esc(v.book.summary)}</p>${button(v.book.guideHref,'Open the study guide')}</article>`,
 'book-guide':v=>`${pageTop(v,'Book study guide')}<div class="lesson-list">${v.chapters.length?v.chapters.map(c=>`<a class="lesson-card" href="${c.href}"><div><h2>${esc(c.title)}</h2><p>${esc(c.introduction)}</p></div>${arrow}</a>`).join(''):`<div class="quiet-panel"><h2>A closer look is on its way.</h2><p>Until chapter companions are available, try a reading activity with this book.</p>${button(v.link('study-guide/reading-practice/'),'Try a reading activity')}</div>`}</div>`,
 chapter:v=>`${pageTop(v,v.book.title)}<article class="prose"><p>${esc(v.chapter.introduction)}</p><h2>Think about it</h2>${questions(v.chapter.questions)}${button(v.chapter.discussionHref,'Discuss this chapter',true)}</article>${note(v,`${v.book.slug}:${v.chapter.slug}`)}<nav class="chapter-navigation" aria-label="Chapter navigation">${button(v.previous?.href??v.book.guideHref,v.previous?'Previous chapter':'Chapter index',true)}${v.next?button(v.next.href,'Next chapter'):button(v.book.guideHref,'Back to the book')}</nav>`,
 'chapter-discussion':v=>`${pageTop(v,`Discuss ${v.book.title}`)}<article class="prose">${questions(v.chapter.discussion??v.chapter.questions)}${button(v.chapter.href,'Return to the chapter',true)}</article>`
};

export function renderPage(v) {
 const nav=[['Books','books/'],['Study guide','study-guide/'],['Themes','themes/'],['Vocabulary','vocabulary/'],['Discussion','discussion/']];
 const active=path=>v.route.startsWith(path);
 return `<!doctype html><html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1"><meta name="description" content="${esc(v.description)}"><meta name="theme-color" content="#172a31"><title>${esc(v.kind==='home'?'Rhyme Study Guide — Dylan’s Books':`${v.title} | Rhyme Study Guide`)}</title><link rel="canonical" href="${v.site.origin}${v.link(v.route)}"><meta property="og:type" content="website"><meta property="og:title" content="${esc(v.title)}"><meta property="og:description" content="${esc(v.description)}"><meta property="og:url" content="${v.site.origin}${v.link(v.route)}">${v.kind==='not-found'?'<meta name="robots" content="noindex">':''}<link rel="icon" type="image/svg+xml" href="${v.link('assets/favicon.svg')}"><link rel="stylesheet" href="${v.link('assets/site.css')}"><script src="${v.link('assets/site.js')}" defer></script></head><body class="page-${v.kind}"><a class="skip-link" href="#main">Skip to content</a><header class="site-header"><a class="brand" href="${v.link('')}" aria-label="Rhyme Study Guide home"><span class="brand-mark" aria-hidden="true">r<span>.</span></span><span>Rhyme<span class="brand-sub">A DYLAN’S BOOKS COMPANION</span></span></a><button class="menu-toggle" type="button" aria-expanded="false" aria-controls="primary-nav">Menu <span aria-hidden="true">☰</span></button><nav id="primary-nav" class="primary-nav" aria-label="Main navigation">${nav.map(([name,path])=>`<a href="${v.link(path)}" ${active(path)?'aria-current="location"':''}>${name}</a>`).join('')}</nav><a class="header-resource" href="${v.link('resources/')}">Resources ${arrow}</a></header><main id="main">${views[v.kind](v)}</main><footer class="site-footer"><div class="footer-top"><a class="brand" href="${v.link('')}"><span class="brand-mark" aria-hidden="true">r<span>.</span></span><span>Rhyme<span class="brand-sub">STAY CURIOUS.</span></span></a><p>Stories are only the beginning.</p><nav aria-label="Footer navigation"><a href="${v.link('about/author/')}">About Dylan</a><a href="${v.link('about/project/')}">About the guide</a><a href="${v.link('resources/')}">Resources</a></nav></div><div class="footer-bottom"><span>Dylan’s Books · Rhyme Study Guide</span><button class="motion-toggle" data-motion-toggle type="button" aria-pressed="false">Motion: on</button><span>Made with curiosity · Luminary Labs</span></div></footer><noscript><style>[data-prompt-index],[data-next-prompt],.motion-toggle,.print-toolbar,.menu-toggle,.note-actions{display:none!important}.primary-nav{display:flex!important}textarea[data-note]{display:none}</style><p class="noscript-note">Reading pages work without JavaScript. Local notes and interactive prompts need JavaScript; use a printable sheet instead.</p></noscript></body></html>`;
}
