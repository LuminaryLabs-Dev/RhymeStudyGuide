import test from 'node:test';
import assert from 'node:assert/strict';
import {volumes} from '../src/domains/volumes/catalog.mjs';
import {ExplorerViewModel} from '../src/domains/volumes/view-model.mjs';
test('volume navigation has boundaries and invalid selection cannot corrupt state',()=>{const vm=new ExplorerViewModel(volumes);assert.equal(vm.snapshot().volume.id,1);vm.select(0);assert.equal(vm.step(-1),false);assert.equal(vm.snapshot().canPrevious,false);vm.select(11);assert.equal(vm.step(1),false);assert.equal(vm.snapshot().canNext,false);for(const invalid of [-1,12,NaN,1.5,'2'])assert.equal(vm.select(invalid),false);assert.equal(vm.snapshot().volume.id,12);});
test('hash navigation restores selection and handles invalid volume hashes',()=>{const vm=new ExplorerViewModel(volumes,'#volume-03');assert.equal(vm.snapshot().volume.id,3);vm.readHash('#volume-12');assert.equal(vm.hash,'#volume-12');vm.readHash('#all-volumes');assert.equal(vm.snapshot().volume.id,12);vm.readHash('#volume-99');assert.equal(vm.snapshot().volume.id,1);});
test('companion focuses cannot masquerade as approved book summaries',()=>{assert.equal(volumes.length,12);for(const v of volumes){assert.equal(v.bookSummary,null);assert.equal(v.bookTitle,null);assert.equal(v.status,'reading-companion');assert.ok(v.summary.length>80);assert.ok(!v.route.startsWith('/'));}});
import {WheelSelection} from '../src/domains/volumes/input.mjs';
test('trackpad bursts select once, reset after silence, and ignore small jitter',()=>{const wheel=new WheelSelection();assert.equal(wheel.feed(20,0),0);assert.equal(wheel.feed(-10,10),0);assert.equal(wheel.feed(40,20),0);assert.equal(wheel.feed(40,40),1);for(let time=60;time<1200;time+=20)assert.equal(wheel.feed(100,time),0);assert.equal(wheel.feed(-90,1500),-1);});
test('view changes preserve selected volume and reject unrelated views',()=>{const vm=new ExplorerViewModel(volumes,'#volume-07');vm.show('resources');assert.equal(vm.hash,'#resources');assert.equal(vm.snapshot().volume.id,7);assert.equal(vm.show('missing'),false);vm.show('volumes');assert.equal(vm.hash,'#volume-07');});
